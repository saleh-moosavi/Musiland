"use client";

export default function AddComment({ id: songId }: { id: string }) {
  return (
    <section className="w-full flex justify-center text-my-black-max dark:text-my-white-low">
      Add Comment Section
    </section>
  );
}
